# plugins/evo/.codex-plugin/

| File | Purpose |
|------|---------|
| `plugin.json` | Plugin manifest read by Codex on install. Same role as `.claude-plugin/plugin.json`, just for the Codex host. Version must match the CLI. |
