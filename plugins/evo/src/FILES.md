# plugins/evo/src/

Standard `src/` layout for the Python package. The actual code lives one
level deeper.

| Subdir | Purpose |
|--------|---------|
| `evo/` | The `evo` Python package: CLI, workspace state, dashboard, dispatch, frontier strategies, locking. |
