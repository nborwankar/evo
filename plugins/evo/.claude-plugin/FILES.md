# plugins/evo/.claude-plugin/

| File | Purpose |
|------|---------|
| `plugin.json` | Plugin manifest read by Claude Code on install. Carries the version string Claude Code uses to decide whether to refetch the plugin. **Must stay in lockstep with the CLI version** — see `docs/ARCHITECTURE.md` § Versioning. |
