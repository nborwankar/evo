# .agents/

Marketplace metadata so generic-agent hosts can discover the plugin from a
local checkout.

| Entry | Purpose |
|-------|---------|
| `plugins/` | Directory holding the marketplace JSON consumed by generic-agent hosts. See `.agents/plugins/FILES.md`. |
