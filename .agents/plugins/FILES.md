# .agents/plugins/

| File | Purpose |
|------|---------|
| `marketplace.json` | Generic agent-host marketplace manifest. Names the marketplace `evo-hq` and points to `./plugins/evo` as the local source for the `evo` plugin. Used by hosts that consume the `.agents/` discovery format. |
