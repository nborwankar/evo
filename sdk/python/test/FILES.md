# sdk/python/test/

Python SDK tests. Mirrors `sdk/node/test/run.test.js` for parity.

| File | Purpose |
|------|---------|
| `test_run.py` | Unit tests for `Run`, `Gate`, and `LocalBackend`. Run as `python3 sdk/python/test/test_run.py` (CI invokes it as a script, not via pytest discovery). |
